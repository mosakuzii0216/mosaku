{
  "name": "mosaku",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -k -n back,front -c blue,magenta \"npm run -w backend start:dev\" \"npm run -w frontend dev\"",
    "test": "npm run --workspaces --if-present test"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
