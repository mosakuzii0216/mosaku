# CLAUDE.md

## Claudeとの契約

線引きは「コードを書くか」ではなく「**そこに判断があるか**」。

| 種類                          | Claude                       | 人間                     |
| ----------------------------- | ---------------------------- | ------------------------ |
| ボイラープレート（正解が1つ） | 丸ごと出す                   | タイプして動かす         |
| 設計判断                      | 選択肢・trade-off・推奨を1つ | 決める。理由ごと記録する |
| ロジック・テスト              | 骨組みと TODO                | 中身を埋める             |
| 環境構築・エラー解読          | 手順を出す                   | 実行する                 |

- Claude はファイルを直接編集しない。人間がタイプする
- **詰まったら30秒で全部出す。**粘らせない（時間が有限だから）
- 「今それを決める必要があるか」を先に問う
- 選択肢を並べるより、1つ推して違ったら言ってもらう
- 隙間時間に投げられた思いつきは議論せず箱に入れる

## 進め方

- 1夜45分。タイマーが鳴ったら途中でも切る。余っても翌日の分を先取りしない。
- 最後に LOG.md へ2行（今日の記録/明日やること1行)
- KPI は1つだけ：**LOG.md を開かない夜を作らない**

## 技術スタック

- frontend: React + TypeScript + Vite（テストは Vitest）
- backend: NestJS + Prisma + PostgresSQL（未着手）

## 決めたこと

- メモテーブルにタイトルカラムを追加した。日記のノートブック内のメモのタイトルは日付を入れたいから。
- Prismaモックを使用せずに実DBを使ってテストする。モックだとカラム名間違いや必須カラムを渡し忘れたりテストの意味が無いから。
- 現在のユーザは@CurrentUser() デコレータ経由で取る。仮実装はcurrent-user.decorator.tsの中だけ。認証導入時もそこ一箇所の変更で済むから。
- frontendとbackendの別オリジン問題はCORSで許可を出す方法にした。設定が楽だから。

  （設計判断を下すたびに、理由ごと1行ずつ足す）

書かないもの

- v1 のアーキテクチャ不変則 - まだ何も決めてない。決めてから足す
- 七夜の計画 - artifact にあるので重複させない
- 詳細な開発コマンド - まだ npm run dev しかない
