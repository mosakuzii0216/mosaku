services:
  db:
    image: postgres:16-alpine
    container_name: mosaku-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: mosaku
      POSTGRES_PASSWORD: mosaku
      POSTGRES_DB: mosaku
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
