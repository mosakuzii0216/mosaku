# CLAUDE.md

## Claudeとの契約

線引きは「コードを書くか」ではなく「**そこに判断があるか**」。

| 種類                          | Claude                       | 人間                     |
| ----------------------------- | ---------------------------- | ------------------------ |
| ボイラープレート（正解が1つ） | 丸ごと出す                   | タイプして動かす         |
| 設計判断                      | 選択肢・trade-off・推奨を1つ | 決める。理由ごと記録する |
| ロジック・テスト              | 骨組みと TODO                | 中身を埋める             |
| 環境構築・エラー解読          | 手順を出す                   | 実行する                 |

- Claude はファイルを直接編集しない。人間がタイプする
- **詰まったら30秒で全部出す。**粘らせない（時間が有限だから）
- 「今それを決める必要があるか」を先に問う
- 選択肢を並べるより、1つ推して違ったら言ってもらう
- 隙間時間に投げられた思いつきは議論せず箱に入れる

## 進め方

- 毎日30分。**タイマーが鳴ったら途中でも切る。**
  - 載っている日は翌日以降の作業を続けてよい。
- タスクは **3日単位の塊**で置いてよい。ただし**各日の終わりにcommitできる状態**で切る。
  (塊のタスクの単位であって、コミットの単位ではない)
- KPIは一つだけ：**LOG.mdをpushしない日を作らない**
- **写経スタイルは維持する。**ボイラープレートを写して動かし、エラーを自分で踏む。
  手を動かす感覚の中で理解が進むことを1週目で確認した。
- 最後に LOG.md へ2行（今日の記録/明日やること1行)
- **このプロジェクトは最終的にmosaku.jpにデプロイする。学習用の使い捨てでは無い。**
  - そのため、本番に出す前提でコードを書く（認証・データ移行・運用を後回しにしない。

## 技術スタック

- frontend: React + TypeScript + Vite（テストは Vitest）
- backend: NestJS + Prisma + PostgresSQL（未着手）

## 決めたこと

- メモテーブルにタイトルカラムを追加した。日記のノートブック内のメモのタイトルは日付を入れたいから。
- Prismaモックを使用せずに実DBを使ってテストする。モックだとカラム名間違いや必須カラムを渡し忘れたりテストの意味が無いから。
- 現在のユーザは@CurrentUser() デコレータ経由で取る。仮実装はcurrent-user.decorator.tsの中だけ。認証導入時もそこ一箇所の変更で済むから。
- frontendとbackendの別オリジン問題はCORSで許可を出す方法にした。設定が楽だから。
- 匿名ユーザは初回アクセス時に発行する。「IDがまだ無いGET」の分岐を作らないため。ゴミユーザは後から掃除できるが、分岐は後から消せない。
- Memo.userIdを外部キーに昇格した。実DBテストなので、存在しないIDをDBが弾いてくれる価値の方が高いから。
- ローカルDBは5432・DB名mosakuに統一し、v1のローカル環境を棄却した。名前で迷う理由をなくすため。:> [!WARNING]
- テスト(backendのjest)は、--runInBandで直列実行する。実DBを1つ共有しているので、並列実行と「テスト間で全消し」は両立しないから。
- 匿名IDはHttpOnly：Cookie(mosaku_uid)で持つ。メアド無しの本登録=パスキーもサーバセッション前提なので、Cookieを先に敷けば後で足すだけで済むから。
- 本登録はパスキーで行い、メールアドレスは必須にしない。ただし1ユーザが複数のパスキーを持てる形にする。日本はwindows PC + iphoneの組み合わせが多く、エコシステムを跨ぐとパスキーが同期されないから。
- frontendのテスト環境はhappy-domを選んだ。vitestの推奨でjsdomより5~10倍速いらしい。詰まったらenvironment一行で変えられるらしい。

  （設計判断を下すたびに、理由ごと1行ずつ足す）

書かないもの

- v1 のアーキテクチャ不変則 - まだ何も決めてない。決めてから足す
- 七夜の計画 - artifact にあるので重複させない
- 詳細な開発コマンド - まだ npm run dev しかない
